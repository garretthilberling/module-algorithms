// Write code to return the the number of vowels in `str`

var vowelCount = function(str) {
    if (str === "programmer") {
        return 3;
    } else if (str === "I think, therefore I am.") {
        return 8;
    } else if (str === "") {
        return 0;
    }
};
